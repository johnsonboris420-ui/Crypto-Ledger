kind = "web"
previewPath = "/"
title = "Crypto Blockchain Dashboard"
version = "1.0.0"
id = "artifacts/crypto-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20512

[services.development]
run = "pnpm --filter @workspace/crypto-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/crypto-dashboard", "run", "build" ]
publicDir = "artifacts/crypto-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20512"
BASE_PATH = "/"
